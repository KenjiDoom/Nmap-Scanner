
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path
from tkinter import *
from tkinter import simpledialog
import nmap

OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"/home/kenji/Videos/build/assets/frame0")


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


window = Tk()

window.geometry("609x408")
window.configure(bg = "#FFFFFF")


canvas = Canvas(
    window,
    bg = "#FFFFFF",
    height = 408,
    width = 609,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)

entry_image_1 = PhotoImage(
    file=relative_to_assets("entry_1.png"))

entry_bg_1 = canvas.create_image(
    202.5,
    292.5,
    image=entry_image_1
)
entry_1 = Entry(
    bd=0,
    bg="#D9D9D9",
    fg="#000716",
    highlightthickness=0
)
entry_1.place(
    x=94.0,
    y=274.0,
    width=217.0,
    height=35.0
)

entry_image_2 = PhotoImage(
    file=relative_to_assets("entry_2.png"))

entry_bg_2 = canvas.create_image(
    450.5,
    292.5,
    image=entry_image_2
)
entry_2 = Entry(
    bd=0,
    bg="#D9D9D9",
    fg="#000716",
    highlightthickness=0
)
entry_2.place(
    x=391.0,
    y=274.0,
    width=119.0,
    height=35.0
)

canvas.create_text(
    423.0,
    253.0,
    anchor="nw",
    text="Port range",
    fill="#000000",
    font=("Inter", 12 * -1)
)

canvas.create_text(
    136.0,
    253.0,
    anchor="nw",
    text="Enter the IP to scan:",
    fill="#000000",
    font=("Inter", 12 * -1)
)

button_image_1 = PhotoImage(
    file=relative_to_assets("button_1.png"))

button_1 = Button(
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    command=lambda: scanner(target=entry_1.get(), port_number=entry_2.get()),
    relief="flat"
)
button_1.place(
    x=145.0,
    y=340.0,
    width=115.0,
    height=36.70587921142578
)

button_image_2 = PhotoImage(
    file=relative_to_assets("button_2.png"))

button_2 = Button(
    image=button_image_2,
    borderwidth=0,
    highlightthickness=0,
    command=lambda: dialog_op(),
    relief="flat"
)
button_2.place(
    x=395.0,
    y=340.0,
    width=115.0,
    height=42.0
)

entry_image_3 = PhotoImage(
    file=relative_to_assets("entry_3.png"))

entry_bg_3 = canvas.create_image(
    299.5,
    126.5,
    image=entry_image_3
)
entry_3 = Text(
    bd=0,
    bg="#E6E6E6",
    fg="#000716",
    highlightthickness=0
)
entry_3.place(
    x=45.0,
    y=15.0,
    width=509.0,
    height=221.0
)

def dialog_op():
    args = simpledialog.askstring("Custom Nmap Arguments", "Enter custom arguments:")
    scanner(target=entry_1.get(), port_number=entry_2.get(), custom_arguments=args)

def scanner(target, port_number, custom_arguments=None):
    
    # Determine if an argument was specified or not
    if custom_arguments == None:
        options = '-sV -O '
    elif custom_arguments:
         # Display the custom arguments? within the json
        options = '-sV -O ' + custom_arguments
    
    nmap_scanner = nmap.PortScanner()
    results = nmap_scanner.scan(target, str(port_number), arguments=options)
    data = results['scan'][str(target)]['tcp']
    
    for port_number in data:
        try:
            product = results['scan'][str(target)]['tcp'][port_number]['product']
            version = results['scan'][str(target)]['tcp'][port_number]['version']
            OS = results['scan'][str(target)]['tcp'][port_number]['cpe']
            script_results = results['scan'][str(target)]['tcp'][port_number]['script']
            
            entry_3.insert(END, str(port_number) + ': ' + results['scan'][str(target)]['tcp'][port_number]['state'] + ' ' + product + ' ' + version + ' ' + OS + '\n')
            
            for key in script_results.keys():
                entry_3.insert(END, key + ": " + script_results[key])
        except KeyError:
            pass


window.resizable(False, False)
window.mainloop()
